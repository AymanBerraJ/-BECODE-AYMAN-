import React from "react";

export default function Form({ }) {
    return (
        <div>
        <input
          type="text"
          placeholder="Emoji..."
        />
        <button>search</button>
      </div>
        
    );
  }